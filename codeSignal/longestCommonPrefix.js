// Leetcode 14

const compare = (wordA, wordB) => {
  let smaller = wordA;
  let longer = wordB;
  if (wordA.length > wordB.length) {
    longer = wordA;
    smaller = wordB;
  }
};

const longestPrexix = (arr) => {
  const prefix = '';

  return prefix;
};

console.log(`floor`.indexOf('flo'));
